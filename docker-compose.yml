version: '3.0'
services:
  app:
    build: .
    command: ['sh', './test.sh']
    volumes:
      - .:/workdir
  scheduler:
    build: .
    command: ['dask-scheduler', '--host', '0.0.0.0']
  worker:
    build: .
    command: ['dask-worker', 'scheduler:8786', '--nthreads', '1']
